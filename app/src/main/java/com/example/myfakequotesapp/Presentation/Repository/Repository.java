package com.example.myfakequotesapp.Presentation.Repository;

import android.app.Application;

import com.example.myfakequotesapp.Presentation.Repository.Room.QuoteRepository;

public class Repository {
    /*static RepositoryTasks repository;

    static public void init(Application application) {
        if (repository == null) {
            repository = new QuoteRepository(application);
        }
    }

    static public RepositoryTasks getRepository() {
        if (repository == null) {
            return null;
        }
        return repository;
    }*/
}
